import { CloudArrowUp, CodeSimple, ToggleLeft, PaintBucket, DeviceMobile, GitFork } from "phosphor-react-native";
import { QUIZ } from "./quiz";

export const QUIZZES = [
  {
    id: "1",
    subtitle: "I.",
    title: QUIZ.find((item) => item.id === "1")?.title,
    description: QUIZ.find((item) => item.id === "1")?.description,
    svg: ToggleLeft,
    pytjet: QUIZ.find((item) => item.id === "1")?.questions.length,
  },
  {
    subtitle: "II a.",
    id: "2",
    title: QUIZ.find((item) => item.id === "2")?.title,
    description: QUIZ.find((item) => item.id === "2")?.description,
    svg: CodeSimple,
    pytjet: QUIZ.find((item) => item.id === "2")?.questions.length,
  },
  {
    subtitle: "II b.",
    id: "3",
    title: QUIZ.find((item) => item.id === "3")?.title,
    description: QUIZ.find((item) => item.id === "3")?.description,
    svg: CodeSimple,
    pytjet: QUIZ.find((item) => item.id === "3")?.questions.length,
  },
  {
    subtitle: "II c.",
    id: "4",
    title: QUIZ.find((item) => item.id === "4")?.title,
    description: QUIZ.find((item) => item.id === "4")?.description,
    svg: CodeSimple,
    pytjet: QUIZ.find((item) => item.id === "4")?.questions.length,
  },
  {
    subtitle: "II d.",
    id: "5",
    title: QUIZ.find((item) => item.id === "5")?.title,
    description: QUIZ.find((item) => item.id === "5")?.description,
    svg: CodeSimple,
    pytjet: QUIZ.find((item) => item.id === "5")?.questions.length,
  },
  {
    subtitle: "III.",
    id: "6",
    title: QUIZ.find((item) => item.id === "6")?.title,
    description: QUIZ.find((item) => item.id === "6")?.description,
    svg: GitFork,
    pytjet: QUIZ.find((item) => item.id === "6")?.questions.length,
  },
  {
    subtitle: "IV.",
    id: "7",
    title: QUIZ.find((item) => item.id === "7")?.title,
    description: QUIZ.find((item) => item.id === "7")?.description,
    svg: PaintBucket,
    pytjet: QUIZ.find((item) => item.id === "7")?.questions.length,
  },
  {
    subtitle: "V.",
    id: "8",
    title: QUIZ.find((item) => item.id === "8")?.title,
    description: QUIZ.find((item) => item.id === "8")?.description,
    svg: PaintBucket,
    pytjet: QUIZ.find((item) => item.id === "8")?.questions.length,
  },
  {
    subtitle: "VI.",
    id: "9",
    title: QUIZ.find((item) => item.id === "9")?.title,
    description: QUIZ.find((item) => item.id === "9")?.description,
    svg: PaintBucket,
    pytjet: QUIZ.find((item) => item.id === "9")?.questions.length,
  },
  {
    subtitle: "VII.",
    id: "10",
    title: QUIZ.find((item) => item.id === "10")?.title,
    description: QUIZ.find((item) => item.id === "10")?.description,
    svg: PaintBucket,
    pytjet: QUIZ.find((item) => item.id === "10")?.questions.length,
  },
];
